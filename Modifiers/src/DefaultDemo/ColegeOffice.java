package DefaultDemo;

public class ColegeOffice {
    String internalNotice="Ummang Started from 9 to 12 of jan";
}

//In that default string  variable internalNotice can be access in same package
// means maine ek dusra class banaya like Student  so main uss class Student me default variable ko access kar sakta hoon

// *********we can access default variable in same package
// *********But we can't access the default variable in another package
